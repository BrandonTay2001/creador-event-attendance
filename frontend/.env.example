# Supabase Configuration
REACT_APP_SUPABASE_URL=your_supabase_project_url
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key

# Microsoft Azure Configuration (for Graph API email sending)
# Note: These are automatically configured through Supabase Auth when users sign in with Microsoft
# The access token with Mail.Send permissions is obtained through the OAuth flow
# Ensure your Azure app registration includes the following scopes:
# - Mail.Send (to send emails)
# - User.Read (to read user profile)